torch>=2.0
torchvision>=0.15
ultralytics==8.3.38
ultralytics-thop
timm>=1.0
transformers>=4.40
py-cpuinfo
GPUtil
psutil
# numpy<2 + opencv<4.10: numpy 2.2.x ships a broken numpy.testing._private.utils
# that imports numpy._core.tests._natype (not in wheels) — every scipy.optimize
# touch (RT-DETR head, transformers DETR/Segformer modeling) crashes via that
# chain. opencv-python 4.10+ requires numpy>=2.
numpy<2
opencv-python<4.10
